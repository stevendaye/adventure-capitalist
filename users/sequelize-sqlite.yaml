dbname: adventure-capitalist-users
username: gc_adcap
password: gc_ac
params:
  dialect: sqlite
  dialectOptions:
    supportBigNumbers: true
  storage: ./database/users-sequelize.sqlite3
  operatorAliases:
    $eq: Op.eq
    $ne: Op.ne
    $gte: Op.gte
    $gt: Op.gt
    $lte: Op.lte
    $lt: Op.lt
    $or: Op.or